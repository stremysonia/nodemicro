import { AppRegistry } from 'react-native';
import App from './src/app.js'; // Import path relative to index.native.js

// Register the App component for the app
AppRegistry.registerComponent('weCookin!', () => App);

